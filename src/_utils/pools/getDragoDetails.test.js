const contractName = 'Drago'

// Test
//

describeContract(contractName, () => {
  describe('getDragoDetails test', () => {
    it('Correctly getDragoDetails test', async () => {
      const ownerAddress = true
      console.log('ok')
      expect(ownerAddress).toBe(true)
    })
  })
})
